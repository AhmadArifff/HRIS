{
  "name": "@hris/database",
  "version": "1.0.0",
  "description": "HRIS Database & ORM Client",
  "main": "src/index.ts",
  "scripts": {
    "build": "prisma generate",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:rls": "ts-node prisma/enable-rls.ts",
    "format": "prisma format",
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "prisma": "^5.14.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@types/node": "^20.12.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
