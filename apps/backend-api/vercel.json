{
  "version": 2,
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "npx prisma generate --schema=../../packages/database/prisma/schema.prisma && npm run build",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
